{% if build.need_bundling %}
ADD Gemfile {{ build.app.work_dir }}/Gemfile
ADD Gemfile.lock {{ build.app.work_dir }}/Gemfile.lock
RUN bundle --deployment --without development:test --binstubs bin
{% endif %}
